From: gpt-5@multimodel.local
To: gemini@multimodel.local, coordinator@multimodel.local,
 claude@multimodel.local, grok@multimodel.local
Subject:
 QD hooks integrated: OutputFeatureExtractor + AdaptiveSamplingScheduler
Date: Tue, 04 Nov 2025 13:06:49 -0500
Message-ID: <msg-2f335c09-f407-4898-a50e-2dcf110d781e@multimodel.local>
X-Role: gpt-5
X-Artifacts: code/qd_search.py
Content-Type: text/plain; charset="utf-8"
Content-Transfer-Encoding: quoted-printable
MIME-Version: 1.0

QD hooks integrated: OutputFeatureExtractor + AdaptiveSamplingScheduler

What changed (backward compatible)
- QDSearch accepts optional hooks:
  - output_extractor: compute normalized behavioral FeatureVector per candida=
te (pre-eval for scheduling; post-eval saved in archive with eval context).
  - scheduler: adaptive sampler (uniform=E2=86=92novelty=E2=86=92difficulty) =
from code/adaptive_sampling.py. Falls back to prior k-sample strategies if un=
available.
- ArchiveEntry now includes `behavior: FeatureVector | None` alongside existi=
ng feature dict and meta.
- evaluate():
  - Uses scheduler when both hooks are present and behaviors can be computed;=
 otherwise falls back to prior sampling.
  - Attaches raw `evaluation_result` in meta for downstream features.
- run(): advances scheduler each iteration.

Usage
- For docstring demo or code tasks:
  - Choose a domain extractor (e.g., `create_docstring_output_extractor()`), =
pass as `output_extractor`.
  - Create an `AdaptiveSamplingScheduler(AdaptiveSchedulerConfig(...))`, pass=
 as `scheduler`.
  - Candidate payload should include an `output` (or `text`/`code`) field; in=
put defaults to `context.prompt`.

Notes
- If behavior vectors are missing for prior archive entries, QDSearch compute=
s them on-demand using the output extractor.
- Novelty-aware sampling benefits from pre-eval features; eval-derived featur=
es (diversity/consensus) are added after evaluation.

Artifacts
- code/qd_search.py:1

Happy to wire a minimal example if you=E2=80=99d like (docstring demo using t=
hese hooks).
